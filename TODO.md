# Light Mode Update for Intern Time Logs

## Plan
1. Update `intern_main.dart` — pass `isDarkMode` prop into `InternTimeLogsPage`
2. Update `intern_time_logs.dart` — accept `isDarkMode` as prop, remove hardcoded `true`, use `AppTheme`
3. Update `intern_time_log_stats_cards.dart` — use `AppTheme` colors
4. Update `intern_time_log_filters.dart` — use `AppTheme` colors
5. Update `intern_time_log_table.dart` — use `AppTheme` colors
6. Update `intern_time_log_pagination.dart` — use `AppTheme` colors
7. Update `intern_time_log_legend.dart` — use `AppTheme` colors

## Status
- [x] Step 1: intern_main.dart
- [x] Step 2: intern_time_logs.dart
- [x] Step 3: intern_time_log_stats_cards.dart
- [x] Step 4: intern_time_log_filters.dart
- [x] Step 5: intern_time_log_table.dart
- [x] Step 6: intern_time_log_pagination.dart
- [x] Step 7: intern_time_log_legend.dart

